[tasks.service-dev]
command = "cargo"
args = ["watch", "-x", "run --bin user-service --verbose"]

[tasks.docs-dev]
command = "cargo"
args = ["watch", "-x", "run --bin user-docs"]

[tasks.dev]
description = "Run Service + Doc server"
script_runner = "bash"
script = [
  "cargo watch -x 'run --bin user-service' | sed 's/^/[SERVICE] /' &",
  "cargo watch -x 'run --bin user-docs'    | sed 's/^/[DOCS]    /' &",
  "wait"
]